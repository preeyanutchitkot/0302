// ในไฟล์ PostScreens.js หรือ PostScreens.tsx
import React from 'react';
import PostScreen from '../screens/Post'; // ตรวจสอบว่าชื่อไฟล์และเส้นทางถูกต้อง

export default function PostScreens() {
  return <PostScreen />;
}
